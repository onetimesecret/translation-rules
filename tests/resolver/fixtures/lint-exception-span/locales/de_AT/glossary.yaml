id: glossary.de_AT
source: SPEC.md
terms:
  - id: term.secret_object#7f3a91c2
    key: secret_object
    en: secret
    de_AT:
      noun:
        target: Geheimnis
    examples:
      - id: ex.standalone-du-with-duden#a12b45ef
        source: You find your secret in the dictionary.
        target: Du findest dein Geheimnis im Duden.
        verdict: good
        note: >-
          SEEDED — standalone forbidden 'du' (sentence-initial) plus the
          allowlisted 'Duden'. The exception must NOT suppress the standalone
          'du' hit just because 'Duden' appears elsewhere in the target;
          the 'du' inside 'Duden' is not a standalone match. Contains the
          sense target 'Geheimnis' so sense_target_presence stays clean —
          forbidden_token is the only finding.
