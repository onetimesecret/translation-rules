id: rules.de
source: SPEC.md
inherits: base
merge_strategy: append
rules:
  - id: rule.de-sie
    modality: MUST
    statement: German content uses the formal Sie form.
    severity: error
    rule_ref: rule.register-lock
