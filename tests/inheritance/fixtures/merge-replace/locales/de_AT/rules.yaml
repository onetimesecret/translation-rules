id: rules.de_AT
source: tests/inheritance/fixtures/merge-replace
inherits: base
merge_strategy: replace
rules:
  - id: rule.child
    modality: MUST
    statement: Child rule.
    severity: error
context:
  - "child context line"
